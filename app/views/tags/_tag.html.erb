<% if tag.id!=nil %>
<div>
   <%= link_to tag.tag_name, tag, class: "tag-button btn btn-success" %> 
   <%= link_to "削除", tag, method: :delete,
                                       data: { confirm: "You sure?" }, class: "delete-tag" %>
</div>
<% end %>